## 0. 变更边界控制

- [x] 0.1 明确本次改动文件范围，仅包含聊天事件映射、聊天渲染与必要持久化代码路径。
- [x] 0.2 为每个改动点做无关模块影响检查，确认未修改非聊天相关模块逻辑。

## 1. 事件模型与映射

- [x] 1.1 审计当前工具/技能回调载荷字段，定位 `arguments` 在进入 UI 映射前丢失的位置。
- [x] 1.2 引入或对齐统一调用事件模型，包含 `callType`、`name`、`arguments`、`status`、`result`、`error`、`timestamp`。
- [x] 1.3 更新回调到 UI 的事件映射器，确保 `onToolCallStart` 的参数原样透传。
- [x] 1.4 确保调用结束更新时保留原始 `arguments`，并追加 `result` 或 `error`。

## 2. 聊天 UI 渲染修复

- [x] 2.1 更新工具/技能调用卡片组件：展示参数载荷，并在参数为空时显示明确的空状态。
- [x] 2.2 为超长参数增加安全截断显示（仅 UI 层），同时保留完整存储内容。
- [x] 2.3 定义并接入悬浮窗颜色 token：`tool-started`、`tool-succeeded`、`tool-failed`、`skill-started`、`skill-succeeded`、`skill-failed`。
- [x] 2.4 重构状态颜色解析逻辑，按 `callType + status` 双维度计算颜色。

## 3. 流式消息缓冲生命周期

- [x] 3.1 重构助手流式处理逻辑：按消息标识分配独立 buffer，替代全局累积。
- [x] 3.2 发出并消费明确的消息完成边界事件，完成后立即清理 buffer。
- [x] 3.3 确保新助手消息初始化从空 buffer 开始，不能继承上一条消息内容。

## 4. 持久化与兼容性

- [x] 4.1 为工具与技能调用统一持久化稳定的 `arguments` 字段。
- [x] 4.2 为历史记录缺失新字段的情况增加兼容处理，避免渲染失败。
- [x] 4.3 验证回放流程可正确重建带参数与最终状态的调用卡片。

## 5. 验证与回归覆盖

- [x] 5.1 增加回调映射单元测试，验证 `arguments` 端到端保真。
- [x] 5.2 增加 UI/状态测试，覆盖悬浮窗在类型与状态组合下的颜色矩阵。
- [x] 5.3 增加流式渲染测试，验证 A/B 消息隔离且最终文本不累积历史内容。
- [x] 5.4 运行定向测试集并记录通过/失败结果与后续修复项。

