<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".PhoneClawApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PhoneClaw">
        
        <!-- Main Chat Activity -->
        <activity
            android:name=".ui.chat.ChatActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PhoneClaw"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tool Call Detail Activity -->
        <activity
            android:name=".ui.chat.ToolCallDetailActivity"
            android:exported="false"
            android:label="工具调用详情"
            android:theme="@style/Theme.PhoneClaw" />

        <!-- Skill Settings Activities -->
        <activity
            android:name=".ui.settings.skill.SkillListActivity"
            android:exported="false"
            android:label="技能管理"
            android:theme="@style/Theme.PhoneClaw" />

        <activity
            android:name=".ui.settings.skill.SkillEditActivity"
            android:exported="false"
            android:label="编辑技能"
            android:theme="@style/Theme.PhoneClaw" />

        <!-- Settings Activity -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:label="@string/settings"
            android:theme="@style/Theme.PhoneClaw" />

        <!-- Provider/Model Settings Activities -->
        <activity
            android:name=".ui.settings.model.ProviderListActivity"
            android:exported="false"
            android:label="@string/model_settings"
            android:theme="@style/Theme.PhoneClaw" />

        <activity
            android:name=".ui.settings.model.ProviderEditActivity"
            android:exported="false"
            android:label="@string/add_provider"
            android:theme="@style/Theme.PhoneClaw" />

        <activity
            android:name=".ui.settings.model.ProviderConfigActivity"
            android:exported="false"
            android:label="@string/provider_config"
            android:theme="@style/Theme.PhoneClaw" />

        <activity
            android:name=".ui.settings.model.ModelEditActivity"
            android:exported="false"
            android:label="@string/add_model"
            android:theme="@style/Theme.PhoneClaw" />

        <!-- Legacy MainActivity (keep for reference) -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.PhoneClaw" />

        <!-- Floating Window Service -->
        <service
            android:name=".ui.floating.FloatingWindowService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- Keepalive Service -->
        <service
            android:name=".service.KeepaliveService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- Boot Receiver -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Accessibility Service -->
        <service
            android:name=".emu.EmuAccessibilityService"
            android:label="PhoneClaw Emulation"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/emu_accessibility_service_config" />
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>